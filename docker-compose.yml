services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: flask-devops-api

    environment:
      APP_ENV: production
      APP_VERSION: 1.0.0
      PORT: 5000

    ports:
      - "5000:5000"

    restart: unless-stopped